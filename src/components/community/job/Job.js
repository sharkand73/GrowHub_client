import React from 'react';

// The purpose of this file is to render one jobbie

// Incoming props = Jobs

const Job = () =>{
    
    return (
        <>
            <h1>This is one jobbie</h1>
        </>
    );
    }

export default Job;